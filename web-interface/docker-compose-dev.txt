services:
  web-interface:
    build: .
    ports:
      - "3000:3000"
    environment:
      - FLASK_ENV=production
    depends_on:
      - cli-server
    networks:
      - hpta-network
    restart: unless-stopped

  cli-server:
    build: ..
    ports:
      - "5001:5000"
    volumes:
      - ../categories:/app/categories
    networks:
      - hpta-network
    restart: unless-stopped

networks:
  hpta-network:
    driver: bridge 